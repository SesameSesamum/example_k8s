apiVersion: v1
kind: ServiceAccount
metadata:
  name: hello-world
  namespace: hello-world
automountServiceAccountToken: false
# ghcr-pull-secret is created manually with a read:packages-only PAT, never committed
imagePullSecrets:
  - name: ghcr-pull-secret
